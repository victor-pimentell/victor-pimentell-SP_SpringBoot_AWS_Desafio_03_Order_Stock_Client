spring:
  application:
    name: stockms

  datasource:
    url: jdbc:postgresql://localhost:5432/stockdb
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true